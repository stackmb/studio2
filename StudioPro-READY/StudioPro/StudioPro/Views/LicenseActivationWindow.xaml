<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="RomanticStudio.Views.LicenseActivationWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                Padding="30,40">
            <StackPanel HorizontalAlignment="Center" Spacing="10">
                <TextBlock Text="🎬" FontSize="48" HorizontalAlignment="Center"/>
                <TextBlock Text="استودیو رویایی" FontSize="28" FontWeight="Bold" 
                          HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" Padding="40,30">
            <StackPanel Spacing="25" MaxWidth="500" HorizontalAlignment="Center">
                
                <Border Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" 
                        CornerRadius="12" Padding="20">
                    <StackPanel Spacing="12">
                        <TextBlock Text="🖥️ شناسه سخت‌افزار" FontWeight="SemiBold" FontSize="14"/>
                        
                        <Grid ColumnSpacing="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBox x:Name="HardwareIdText" IsReadOnly="True" Grid.Column="0"/>
                            <Button x:Name="CopyHwIdButton" Click="CopyHwIdButton_Click" Grid.Column="1">
                                <FontIcon Glyph="&#xE8C8;"/>
                            </Button>
                        </Grid>
                    </StackPanel>
                </Border>

                <StackPanel Spacing="10">
                    <TextBlock Text="کلید لایسنس" FontWeight="SemiBold"/>
                    <TextBox x:Name="LicenseKeyInput" Height="80" TextWrapping="Wrap"/>
                </StackPanel>

                <Button x:Name="ActivateButton" Click="ActivateButton_Click" HorizontalAlignment="Stretch">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <FontIcon Glyph="&#xE73E;"/>
                        <TextBlock Text="فعال‌سازی"/>
                    </StackPanel>
                </Button>

                <ProgressRing x:Name="LoadingRing" IsActive="False" Visibility="Collapsed"/>

                <Border Background="{ThemeResource AccentFillColorTertiaryBrush}" 
                        CornerRadius="12" Padding="20">
                    <StackPanel Spacing="15">
                        <TextBlock Text="📨 درخواست لایسنس" FontWeight="SemiBold"/>
                        <TextBox x:Name="UserNameInput" PlaceholderText="نام شما"/>
                        <Button x:Name="RequestLicenseButton" Click="RequestLicenseButton_Click" HorizontalAlignment="Stretch">
                            <TextBlock Text="ارسال درخواست"/>
                        </Button>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
